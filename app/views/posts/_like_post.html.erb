<%= form_for(Like.new(user: current_user, post: post), url: user_likes_path(current_user)) do |f| %>
  <%= f.hidden_field :user_id %>
  <%= f.hidden_field :post_id %>

  <%= f.submit "Like This Post" %>
<% end %>
